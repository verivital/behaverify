# Training-side dependencies for the A*-net pipeline (a_star_files/).
# These are NOT in the repo's requirements/*.txt; the 2025 retraining effort
# installed them ad hoc into a venv. Versions pinned to the environment the
# 2026-07-02 fixes were validated with (python 3.12, CUDA 12.6).
torch==2.7.1
onnx==1.18.0
onnxruntime==1.22.0
numpy>=1.26
matplotlib>=3.8
