MODULE main
	CONSTANTS
		success, failure, running, invalid;
	DEFINE
		statuses := [root.status, sel5.status, sel3.status, leaf1.status, leaf2.status, sel2.status, leaf3.status, leaf4.status, sel4.status, sel1.status, leaf5.status, leaf6.status, leaf0.status];
		resume_from_node_0 := -3;
		root.active := TRUE;
	VAR
		node_names : define_nodes;
		root : node_selector2(sel5, sel4);
		sel5 : node_selector2(sel3, sel2);
		sel3 : node_selector2(leaf1, leaf2);
		leaf1 : node_default();
		leaf2 : node_default();
		sel2 : node_selector2(leaf3, leaf4);
		leaf3 : node_default();
		leaf4 : node_default();
		sel4 : node_selector2(sel1, leaf0);
		sel1 : node_selector2(leaf5, leaf6);
		leaf5 : node_default();
		leaf6 : node_default();
		leaf0 : node_default();
	ASSIGN
MODULE fake_node()
	CONSTANTS
		success, failure, running, invalid;
	DEFINE
		status := invalid;
MODULE node_default()
	CONSTANTS
		success, failure, running, invalid;
	VAR
		input_status : {success, running, failure};
	DEFINE
		status := active ? input_status : invalid;
MODULE node_selector2(child_0, child_1)
	CONSTANTS
		success, failure, running, invalid;
	DEFINE
		status :=
			case
				!(active) : invalid;
				!(child_0.status = failure) : child_0.status;
				!(child_1.status = failure) : child_1.status;
				TRUE : failure;
			esac;
		child_0.active := active;
		child_1.active := child_0.status = failure;
MODULE define_nodes
	DEFINE
		root := 0;
		sel5 := 1;
		sel3 := 2;
		leaf1 := 3;
		leaf2 := 4;
		sel2 := 5;
		leaf3 := 6;
		leaf4 := 7;
		sel4 := 8;
		sel1 := 9;
		leaf5 := 10;
		leaf6 := 11;
		leaf0 := 12;
