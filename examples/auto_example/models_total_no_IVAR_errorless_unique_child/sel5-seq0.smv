MODULE main
	CONSTANTS
		success, failure, running, invalid;
	DEFINE
		statuses := [root.status, sel4.status, sel2.status, leaf2.status, leaf3.status, sel1.status, leaf4.status, leaf5.status, sel3.status, leaf0.status, leaf1.status];
		resume_from_node_0 := -3;
		root.active := TRUE;
	VAR
		node_names : define_nodes;
		root : node_selector2(sel4, sel3);
		sel4 : node_selector2(sel2, sel1);
		sel2 : node_selector2(leaf2, leaf3);
		leaf2 : node_default();
		leaf3 : node_default();
		sel1 : node_selector2(leaf4, leaf5);
		leaf4 : node_default();
		leaf5 : node_default();
		sel3 : node_selector2(leaf0, leaf1);
		leaf0 : node_default();
		leaf1 : node_default();
	ASSIGN
MODULE fake_node()
	CONSTANTS
		success, failure, running, invalid;
	DEFINE
		status := invalid;
MODULE node_default()
	CONSTANTS
		success, failure, running, invalid;
	VAR
		input_status : {success, running, failure};
	DEFINE
		status := active ? input_status : invalid;
MODULE node_selector2(child_0, child_1)
	CONSTANTS
		success, failure, running, invalid;
	DEFINE
		status :=
			case
				!(active) : invalid;
				!(child_0.status = failure) : child_0.status;
				!(child_1.status = failure) : child_1.status;
				TRUE : failure;
			esac;
		child_0.active := active;
		child_1.active := child_0.status = failure;
MODULE define_nodes
	DEFINE
		root := 0;
		sel4 := 1;
		sel2 := 2;
		leaf2 := 3;
		leaf3 := 4;
		sel1 := 5;
		leaf4 := 6;
		leaf5 := 7;
		sel3 := 8;
		leaf0 := 9;
		leaf1 := 10;
