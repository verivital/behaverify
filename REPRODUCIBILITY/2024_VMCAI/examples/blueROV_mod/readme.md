The pre version updates the environment before ticks, while the post version updates the environment after ticks.
The pre version is much smaller in terms of states, but the post version appears to perform much faster.


This example only works with the norm encoding.
